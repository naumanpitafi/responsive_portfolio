import 'package:flutter/material.dart';

const primaryColor = Color(0xFF504DEE);
const secondaryColor = Color(0xFF2C2B32);
const greyColor = Color(0xFF9E9E9E);
const whiteColor = Color(0xffFFFFFF);
const blackColor = Color.fromARGB(225, 0, 0, 0);

const darkColor = Color(0xFF191923);
const bodyTextColor = Color(0xFF8B8B8D);
const bgColor = Color(0xFF1E1E28);

const defaultPadding = 20.0;
const defaultDuration = Duration(seconds: 1); // we use it on our animation
const maxWidthWindow = 1440.0; // max width of our web
const maxWidthtablet = 1440.0;
const maxWidthmobile = 1440.0;

const poppins = "Poppins";
const poppinsLight = "PoppinsLight";
const urbanist = "Urbanist";
